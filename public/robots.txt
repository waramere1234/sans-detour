# Sans Détour — robots policy
# The app's public-facing pages (Cover, Méthode, Légal) are crawlable.
# Future API endpoints under /api/ would not be — none today.

User-agent: *
Allow: /

# TODO production-blocker: re-enable when public/sitemap.xml is generated
# at build time. Previously declared but the file never existed, so every
# crawler that parsed this hit a 404 on the sitemap fetch.
# Sitemap: https://sansdetour.fr/sitemap.xml
